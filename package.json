{
  "name": "digital-dashboard-root",
  "version": "1.0.0",
  "description": "Digital Dashboard deployment and management scripts",
  "scripts": {
    "deploy": "node scripts/deploy.js",
    "deploy:cms": "node scripts/deploy.js --cms",
    "deploy:backend": "node scripts/deploy.js --backend",
    "deploy:tv": "node scripts/deploy.js --tv",
    "ship": "node scripts/ship.js",
    "ship:all": "node scripts/ship.js --all",
    "ship:tv": "node scripts/ship.js --tv",
    "ship:backend": "node scripts/ship.js --backend",
    "build:all": "cd backend && npm run build && cd ../web-cms && npm run build"
  },
  "dependencies": {
    "basic-ftp": "^5.0.5",
    "dotenv": "^16.4.7"
  }
}
